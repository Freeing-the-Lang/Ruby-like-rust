# 💎 Ruby-like-rust
**“Ruby의 감성 + Rust의 안전성” — 표현력과 성능의 조화**

---

## 📘 개요
Ruby-like-rust는 Ruby의 간결하고 유연한 문법을 기반으로,  
Rust의 **메모리 안전성**과 **컴파일 타임 검사**를 결합한 새로운 언어 실험이다.  
이 프로젝트는 “프로그래머가 사고하는 흐름 그대로 안전한 코드를 작성할 수 있는 언어”를 목표로 한다.  

---

## ⚙️ 핵심 개념
- **Rust 스타일의 Ownership / Borrowing**을 Ruby 문법에 자연스럽게 녹임  
- **Garbage Collector 없는 Ruby 스타일 런타임**  
- **Macro 기반 DSL 지원**으로 문법 확장 가능  
- **LLVM-Free 빌드 경로** 및 ProofLedger 기반 **재현 가능한 컴파일**  

---

## 🧱 예시 코드
```ruby
fn greet(name)
  puts "Hello, #{name}!"
end

let user = "world"
greet(user)
```

→ 컴파일 시 내부적으로 Rust 스타일 타입 검사를 수행  
→ 결과적으로 `out.cpp`, `out.asm`, `proofledger.txt` 생성  

---

## 🧩 구조 (예시)
```
Ruby-like-rust/
 ├── src/
 │    ├── parser_ruby.rs
 │    ├── transpiler_cpp.cpp
 │    └── out.asm
 ├── proofledger.txt
 ├── LICENSE
 ├── README.md
 └── .github/workflows/
      └── pure-rust-no-llvm.yml
```

---

## 🧾 향후 계획
- Ruby → Rust AST 변환기 고도화  
- ProofLedger 자동 생성 및 서명  
- Multi-OS 자동 빌드 및 릴리즈  
- 언어 파이프라인 시각화 추가  

---

## 🪪 라이선스
MIT License © 2025
